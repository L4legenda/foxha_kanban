import { TaskType } from "../model/types";


const select_task: TaskType = {
    name: "",
}

const state = {
    select_task,
}

const mutations = {

}

const actions = {

}

export default {
    state,
    mutations,
    actions,
}